#Detecting-text-in-high-noise-agriculture-beehive-images

## Data augmentation is needed only when we're dealing with a small dataset
## after labeling the images and having the labels in the same folder as the images (using labelImg tool) put them in a zip file and upload them to the drive
## Training the model should be done in google colab and the results should be saved on the drive

